const sectionHover = new SectionHover();

sectionHover.sectionNavHover();

const mobNav = new MobNav();

mobNav.menuIcon();

const scrollMenu = new ScrollMenu();

scrollMenu.fixedNavScroll();
scrollMenu.smoothScroll();

const scrollButton = new ScrollButton();

scrollButton.scrollToTop();


